# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'integratedFlutter' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for integratedFlutter

  target 'integratedFlutterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'integratedFlutterUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
#target 'integratedFlutter' do
#  flutter_application_path = '/Users/xiangwenwen/integratedFlutter/my_flutter/'
#  eval(File.read(File.join(flutter_application_path, '.ios', 'Flutter', 'podhelper.rb')), binding)
#end

flutter_application_path = '/Users/wilson/Downloads/integratedFlutter/my_flutter'
eval(File.read(File.join(flutter_application_path, '.ios', 'Flutter', 'podhelper.rb')), binding)
